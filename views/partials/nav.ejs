<nav class="nav">
  <a href="/">Gallery</a>
  <a href="/add">Add Photos/Videos</a>
  <a href="/members">Members</a>
  <% if (isTester) { %>
    <a href="/chat">Chat</a>
  <% } %>
  <% if (currentMember) { %>
    <a href="/support">🩹 Support</a>
  <% } %>
  <span class="nav-spacer"></span>
  <% if (isOwner) { %>
    <span class="owner-badge" title="Owner mode is active on this device">Owner</span>
  <% } %>
  <% if (currentMember) { %>
    <a href="/profile" class="nav-member">Hi, <%= currentMember %></a>
  <% } else { %>
    <a href="/join">Join</a>
  <% } %>
</nav>
